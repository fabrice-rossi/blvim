% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_list.R
\name{grid_sim_iterations}
\alias{grid_sim_iterations}
\title{Returns the number of iterations used to produce of a collection of spatial
interaction models}
\usage{
grid_sim_iterations(sim, ...)
}
\arguments{
\item{sim}{a collection of spatial interaction models, an object of class
\code{sim_list}}

\item{...}{additional parameters}
}
\value{
a vector of numbers of iteration, one per spatial interaction model
}
\description{
The function reports for each spatial interaction model of its \code{sim_list}
parameter the number of iterations used to produce it (see \code{\link[=sim_iterations]{sim_iterations()}})
}
\details{
Notice that \code{\link[=sim_iterations]{sim_iterations()}} is generic and can be applied directly to
\code{sim_list} objects. The current function is provided to be explicit in R code
about what is a unique model and what is a collection of models (using
function names that start with \code{"grid_"})
}
\examples{
positions <- matrix(rnorm(15 * 2), ncol = 2)
distances <- as.matrix(dist(positions))
production <- rep(1, 15)
attractiveness <- rep(1, 15)
all_flows <- grid_blvim(distances,
  production,
  c(1.1, 1.25, 1.5),
  c(1, 2, 3),
  attractiveness,
  bipartite = FALSE,
  epsilon = 0.1,
  iter_max = 750,
)
grid_sim_iterations(all_flows)
}
\seealso{
\code{\link[=sim_iterations]{sim_iterations()}}, \code{\link[=grid_sim_converged]{grid_sim_converged()}} and \code{\link[=grid_blvim]{grid_blvim()}}
}
